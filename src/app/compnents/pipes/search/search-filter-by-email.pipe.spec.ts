import { SearchFilterByEmailPipe } from './search-filter-by-email.pipe';

describe('SearchFilterByEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterByEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
