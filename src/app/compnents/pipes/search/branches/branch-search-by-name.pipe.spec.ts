import { BranchSearchByNamePipe } from './branch-search-by-name.pipe';

describe('BranchSearchByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new BranchSearchByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
