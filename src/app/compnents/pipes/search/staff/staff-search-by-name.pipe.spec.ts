import { StaffSearchByNamePipe } from './staff-search-by-name.pipe';

describe('StaffSearchByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new StaffSearchByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
