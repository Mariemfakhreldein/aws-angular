import { RoleSearchByNamePipe } from './role-search-by-name.pipe';

describe('RoleSearchByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new RoleSearchByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
