import { TrackSearchByNamePipe } from './track-search-by-name.pipe';

describe('TrackSearchByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TrackSearchByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
