import { IntakeSearchByNamePipe } from './intake-search-by-name.pipe';

describe('IntakeSearchByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new IntakeSearchByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
